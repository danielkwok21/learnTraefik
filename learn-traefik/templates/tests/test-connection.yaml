apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learn-traefik.fullname" . }}-test-connection"
  labels:
    {{- include "learn-traefik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learn-traefik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
