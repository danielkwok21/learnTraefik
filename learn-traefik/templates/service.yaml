apiVersion: v1
kind: Service
metadata:
  name: {{ include "learn-traefik.fullname" . }}
  labels:
    {{- include "learn-traefik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learn-traefik.selectorLabels" . | nindent 4 }}
