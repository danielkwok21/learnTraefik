apiVersion: v1
kind: Service
metadata:
  name: {{ include "learnTraefik.fullname" . }}
  labels:
    {{- include "learnTraefik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learnTraefik.selectorLabels" . | nindent 4 }}
