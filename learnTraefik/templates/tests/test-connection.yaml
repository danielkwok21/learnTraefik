apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learnTraefik.fullname" . }}-test-connection"
  labels:
    {{- include "learnTraefik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learnTraefik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
